/** @type {import('next').NextConfig} */
const nextConfig = {
    distDir: 'out',
    basePath: '',
    output: 'export',
    images: {
        unoptimized: true
    },
};

export default nextConfig;
